{
	"name": "NextSeed",
	"version": "1.0.0",
	"dependencies": {
		"cors": "^2.8.5",
		"framer-motion": "^6.3.1",
		"gsap": "^3.10.4",
		"next": "^12.1.5",
		"normalize.css": "^8.0.1",
		"react": "^17.0.2",
		"react-dom": "^17.0.2",
		"react-google-recaptcha": "^2.1.0",
		"react-inlinesvg": "^3.0.0",
		"react-redux": "^8.0.1",
		"react-ticker": "^1.3.2",
		"redux": "^4.2.0",
		"redux-devtools-extension": "^2.13.9",
		"redux-thunk": "^2.4.1",
		"sass": "^1.50.1"
	},
	"scripts": {
		"export": "rm -rf out && next build && next export",
		"clean": "rm -rf .next",
		"start": "npm run clean && next dev",
		"build": "npm run clean && next build",
		"prod": "npm run build && next start -p 3107",
		"rb:dev": "ssh -t USER@HOST \"cd /var/www/hue/ && npm run rs:dev\"",
		"rs:dev": "pm2 restart hue_dev",
		"dp:dev": "git push dev main && npm run rb:dev",
		"rb:prod": "ssh -t root@144.126.222.7 \"cd /var/www/hue/ && npm run rs:prod\"",
		"rs:prod": "pm2 restart hue",
		"dp:prod": "git push prod main && npm run rb:prod"
	}
}
